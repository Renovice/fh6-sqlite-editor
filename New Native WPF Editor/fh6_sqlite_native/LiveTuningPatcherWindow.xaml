<Window x:Class="FH6SQLiteEditorNative.LiveTuningPatcherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FH6SQLiteEditorNative"
        Title="Live Tuning Limits"
        Width="1040"
        Height="700"
        MinWidth="860"
        MinHeight="560"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <SolidColorBrush x:Key="DialogBgBrush" Color="#eef2f4"/>
        <SolidColorBrush x:Key="DialogSurfaceBrush" Color="#ffffff"/>
        <SolidColorBrush x:Key="DialogSoftBrush" Color="#f6f8f9"/>
        <SolidColorBrush x:Key="DialogLineBrush" Color="#d6e0e4"/>
        <SolidColorBrush x:Key="DialogTextBrush" Color="#142025"/>
        <SolidColorBrush x:Key="DialogMutedBrush" Color="#64747b"/>
        <SolidColorBrush x:Key="DialogAccentBrush" Color="#086f83"/>
        <SolidColorBrush x:Key="DialogSelectionBrush" Color="#c8eaf0"/>
        <Style x:Key="DialogButton" TargetType="{x:Type Button}">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{DynamicResource DialogTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource DialogSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DialogLineBrush}"/>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Foreground" Value="{DynamicResource DialogTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource DialogSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DialogLineBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource DialogTextBrush}"/>
        </Style>
        <Style TargetType="{x:Type DataGrid}">
            <Setter Property="Foreground" Value="{DynamicResource DialogTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource DialogSurfaceBrush}"/>
            <Setter Property="RowBackground" Value="{DynamicResource DialogSurfaceBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource DialogSoftBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DialogLineBrush}"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DialogLineBrush}"/>
            <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource DialogLineBrush}"/>
        </Style>
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Foreground" Value="{DynamicResource DialogTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource DialogSoftBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DialogLineBrush}"/>
        </Style>
        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="Foreground" Value="{DynamicResource DialogTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource DialogSurfaceBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource DialogAccentBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootGrid" Margin="16" Background="{DynamicResource DialogBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="116"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock x:Name="TitleText" Text="Live Tuning Limits" FontSize="22" FontWeight="SemiBold"/>
                <TextBlock x:Name="IntroText"
                           Text="Scans the running game for global tuning clamps, then patches only when Apply is pressed. Changes are live-only and reset when the game reloads these settings."
                           TextWrapping="Wrap"
                           Margin="0,4,20,10"/>
            </StackPanel>
            <WrapPanel Grid.Column="1" VerticalAlignment="Top">
                <Button x:Name="ScanButton" Content="Scan Current" Style="{StaticResource DialogButton}" Click="ScanButton_Click"/>
                <Button x:Name="RestoreDefaultsButton" Content="Defaults" Style="{StaticResource DialogButton}" Click="RestoreDefaultsButton_Click"/>
                <Button x:Name="ApplyButton" Content="Apply to Game" Style="{StaticResource DialogButton}" Click="ApplyButton_Click"/>
                <Button Content="Close" Style="{StaticResource DialogButton}" Click="CloseButton_Click"/>
            </WrapPanel>
        </Grid>

        <DataGrid x:Name="LimitsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  Margin="0,8,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Limit" Binding="{Binding Name}" Width="210" IsReadOnly="True"/>
                <DataGridTextColumn Header="Current Min" Binding="{Binding CurrentMin, StringFormat=0.###}" Width="105" IsReadOnly="True"/>
                <DataGridTextColumn Header="Current Max" Binding="{Binding CurrentMax, StringFormat=0.###}" Width="105" IsReadOnly="True"/>
                <DataGridTextColumn Header="New Min" Binding="{Binding TargetMin, UpdateSourceTrigger=PropertyChanged, StringFormat=0.###}" Width="105"/>
                <DataGridTextColumn Header="New Max" Binding="{Binding TargetMax, UpdateSourceTrigger=PropertyChanged, StringFormat=0.###}" Width="105"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBox x:Name="StatusBox"
                 Grid.Row="2"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"/>
    </Grid>
</Window>
